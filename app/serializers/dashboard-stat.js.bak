import JSONAPISerializer from '@ember-data/serializer/json-api';

export default class DashboardStatSerializer extends JSONAPISerializer {
    keyForAttribute(attr){
        return attr;
    }
    keyForRelationship(rawKey){
        return rawKey;
    }
}
