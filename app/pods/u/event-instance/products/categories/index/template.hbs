{{page-title (concat "Product Categories for " this.eventInstance.name)}}
<Panels::ProductCategoriesPanel
  @query={{this.productCategoryQuery}}
  @eventInstance={{this.eventInstance}}
  @searchQuery={{this.searchQuery}}
/>