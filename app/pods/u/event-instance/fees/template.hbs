{{page-title 'Fees'}}

    <Forms::EventInstanceFees
      @eventInstance={{this.eventInstance}}
      @eventSeries={{this.eventSeries}}
      @eventOrganiser={{this.eventOrganiser}}
      @selectTab="selectTab"
    />
